# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
# Env files: keep .env.example tracked; ignore every local/backup variant.
# `*.local` above only covers files ending in `.local` (misses e.g.
# `.env.local.spike-bak`), so match the `.env.local` prefix explicitly.
.env
.env.local*
.env.*.local
*.spike-bak
coverage

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local working artifacts (architect plans, scratch notes)
docs/superpowers/
